export enum RoutePath {
    Home = 'home',
    Login = 'login',
    Logout = 'logout',
    AccountSelection = 'dashboard',
    AccountDashboard = 'accounts/:name',
    Balance = 'accounts/:name/balance',
    History = 'accounts/:name/history',
    Plan = 'accounts/:name/plan',
}
